#include<stdio.h>
#include<stdlib.h>
#include<string.h>
typedef struct{
 
	int code;
	char nom[500];
	int quantite;
	float prix;
	
} Produit;

	void stock(char P[i], int n){
 			 P[i]=P[i].prix+(P[i].prix0.15);}
 		
 			    printf("Nom du produit %d : %s \n",i+1,P[i].nom);
        printf("Code du produit %d : %d \n",i+1,P[i].code);
        printf("Quantite du produit %d : %d \n",i+1,P[i].quantite);
        printf("Prix du produit %d : %.2f \n",i+1,P[i].prix);
        printf("Prix du produit %d : %.2f \n",i+1,P[i].ttc);
        printf("\n");
			   }
		 }
	}
